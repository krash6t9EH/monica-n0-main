{
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": ["anonymous"]
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "/info/*",
      "/pitch/*",
      "/*.{png,jpg,jpeg,gif,svg,ico}",
      "/*.css",
      "/*.js"
    ]
  }
}